#!/bin/bash
# OpenOCD config for Raspberry Pi Debug Probe + Pico Plus 2 W (RP2350)

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

openocd \
    -f interface/cmsis-dap.cfg \
    -f target/rp2350.cfg \
    -c "adapter speed 5000" \
    "$@"
